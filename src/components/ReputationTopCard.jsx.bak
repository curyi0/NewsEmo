import React from 'react';
import { Card } from 'antd';

const cardStyle = {
  flex: '1 0 430px',            // ✅ 최소/최대 385px 기반으로 반응
  maxWidth: 450,
  minHeight: 130,
  width: '100%',
  display: 'flex',
  flexDirection: 'column',
  alignItems: 'center',
  justifyContent: 'center',
  textAlign: 'center',
  borderRadius: '20px',
};

const ReputationTopCard = () => {
  return (
    <div
      style={{
        display: 'flex',
        flexWrap: 'wrap',
        gap: '20px',           // 카드 간 간격
        justifyContent: 'center', // 가운데 정렬
      }}
    >
      <Card style={cardStyle}>카드 1</Card>
      <Card style={cardStyle}>카드 2</Card>
      <Card style={cardStyle}>카드 3</Card>
    </div>
  );
};

export default ReputationTopCard;
